package com.personal.job_scheduler.service.executor;

import org.springframework.stereotype.Service;

@Service
public class JobExecutor {
    public void submit() {
        //todo: implement the logic to create proper threads and then submit jobs to them, also the results should be processed for the jobs and updates at db what ever reuqired
    }
}
